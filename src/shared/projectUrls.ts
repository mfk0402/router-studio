/**
 * Canonical public URLs for this repository (Help menu, deep links).
 * After forking, update owner/name here once — package.json repository should match.
 */
export const GITHUB_ORG = 'mfk0402';
export const GITHUB_REPO = 'router-studio';

export const GITHUB_REPO_URL = `https://github.com/${GITHUB_ORG}/${GITHUB_REPO}`;
export const GITHUB_README_URL = `${GITHUB_REPO_URL}#readme`;
/** README “Privacy” section (stable anchor on rendered README). */
export const GITHUB_README_PRIVACY_URL = `${GITHUB_REPO_URL}/blob/main/README.md#privacy`;
export const GITHUB_ISSUES_URL = `${GITHUB_REPO_URL}/issues`;
export const GITHUB_RELEASES_LATEST_URL = `${GITHUB_REPO_URL}/releases/latest`;
