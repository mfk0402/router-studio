# Copy to .env.local (or export in your shell) only for local maintainer / release tooling.
# Never commit real values. `.env`, `.env.local`, and `.env.*` are gitignored.

# Optional: generic update server base URL for electron-updater in packaged builds.
# ROUTER_STUDIO_UPDATES_URL=https://your-cdn.example.com/router-studio/releases
