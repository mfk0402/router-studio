node_modules
out
release
dist
.vite
coverage
*.log
*.tsbuildinfo
.DS_Store
Thumbs.db
ehthumbs.db

# Secrets & local env (never commit)
.env
.env.*
!.env.example
*.pem
*.p12
*.pfx
secrets.local.bin
secrets.local.b64

# IDE / OS
.idea/
*.swp
*~

# Scratch / local experiments (if you use this folder name)
scratch/

# Stray `.js` next to `.ts`/`.tsx` sources (accidental emits; Vite may resolve `.js` first and break the app)
src/**/*.js

# Prefer `electron.vite.config.ts`; avoid duplicate JS beside it
/electron.vite.config.js

# Unrelated local scripts mistakenly placed in repo root (not part of Router Studio)
sol-pnl.ts
